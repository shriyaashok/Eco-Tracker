// TODO: Form to log vehicle trips: date, km, vehicle_type, fuel_type
// TODO: Validate inputs and POST to POST /api/log with payload { type: 'vehicle', payload: {...} }

import React from 'react';

export default function VehicleForm() {
  return (
    <div>
      <h3>Log Vehicle Trip</h3>
      <p>// TODO: form fields: date, km, vehicle_type, fuel_type</p>
    </div>
  );
}
