# EcoTracker Frontend

// TODO: run `npm install` in `frontend/` and `npm start` to run the CRA dev server.

- Frontend implemented with React (create-react-app structure)
- Uses `axios` for HTTP and `recharts` for charts

Commands:

```powershell
cd frontend
npm install
npm start
```
